pipeline {

    agent any

    stages {
      stage(‘Build’) {
        steps {
          
          sh '''
            echo "Dit wordt uitgevoerd in mijn jenkins container"
            echo "Install"
            echo " Test"
            echo "deploy"
            '''
        }
      }
    }
}